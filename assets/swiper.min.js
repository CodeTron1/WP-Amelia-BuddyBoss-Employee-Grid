
/*! swiper wrapper - minimal loader */
(function(window){
    if (typeof window.Swiper !== 'undefined') {
        // Swiper already loaded (from local or other source)
        return;
    }
    // no Swiper found here; we leave it to the fallback script to init a native slider
})(window);
